package com.thecat.authentication.service.endpoint;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeAuthenticateIT extends AuthenticateTest {

    // Execute the same tests but in native mode.
}